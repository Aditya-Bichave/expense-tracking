# This folder holds the saved browser storage state (auth session) generated by globalSetup.
# auth-state.json is .gitignored (contains real Supabase tokens).
# This .gitkeep ensures the directory itself is tracked.
